all: build
include $(ORACLE_HOME)/precomp/lib/env_precomp.mk

USERID=inkless/inkless
PROCFLAGS= sqlcheck=full userid=$(USERID) 
EXE=LSI_Map_CvtIns_Manu.exe

DFLAGS=-D_HPUX_SOURCE -D_DEBUG_LOG -D_MANUAL_MODE -DEBUG_MODE

.SUFFIXES: .pc .c .o .typ .h

OBJS=Mapcvt_Insert.o RegistProcess.o InsertDB.o libm.a

.pc.c:
	$(PROC) $(PROCFLAGS) iname=$*

.pc.o:
	$(PROC) $(PROCFLAGS) iname=$*
	$(CC) $(CFLAGS) $(DFLAGS) -c $*.c

.c.o:
	$(CC) $(CFLAGS) $(DFLAGS) -c $*.c

build: 	$(OBJS)
	$(CC) -Aa -o $(EXE) $(OBJS) -L$(LIBHOME) $(LIBCLNTSH) $(LIBSQL) -pthread

clean:
	rm -f *.o
